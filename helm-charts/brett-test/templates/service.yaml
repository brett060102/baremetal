apiVersion: v1
kind: Service
metadata:
  name: {{ include "baremetal-operator.fullname" . }}
  labels:
    {{- include "baremetal-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "baremetal-operator.selectorLabels" . | nindent 4 }}
